[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @matrix-ecommerce/api db:generate && cd apps/api && pnpm build && ls -la dist/ || echo 'Build failed or dist/ is empty'"

[deploy]
startCommand = "cd apps/api && pnpm db:patch:prod && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

